apiVersion: v1
kind: ConfigMap
metadata:
  name: portfolio-app-config
  labels:
    app: portfolio-app
data:
  NODE_ENV: "production"
  PORT: "3000"
  REDIS_HOST: "portfolio-redis"
  REDIS_PORT: "6379"
  LOG_LEVEL: "info"
